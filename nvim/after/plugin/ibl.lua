-- Indent Lines

require("ibl").setup({
    indent = {
        char = "│",   -- clean vertical line
    },
    scope = {
        enabled = false, -- disable active scope highlighting (more minimal)
    },
})
